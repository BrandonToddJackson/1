typer>=0.12
pydantic>=2.6
python-dotenv>=1.0
rich>=13.7
yt-dlp>=2024.8.6
faster-whisper>=1.0

# Optional, for the LLM-enhanced path (dev/test only needs pytest below):
# pip install -r requirements.txt -e ".[llm]"
#
# Other optional extras (see pyproject.toml):
#   .[publish]     -- live Blotato/Ayrshare publishing (requests)
#   .[elevenlabs]  -- ElevenLabs Scribe transcription (requests)
#   .[diarize]     -- local pyannote.audio diarization (torch, large)
#
# graphics/ (HyperFrames) is a separate Node.js project, not pip-installable
# -- see README.md's "Setting up `graphics`" section (`cd graphics && npm install`).
