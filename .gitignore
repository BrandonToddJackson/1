__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Environment / secrets
.env

# Pipeline run outputs (media, transcripts, outbox) - never commit generated media
runs/
learnings.json

# Whisper model cache if downloaded locally into the repo
.cache/
*.mp4
*.wav
*.srt
!fixtures/**

# graphics/ (HyperFrames Node project, scoped to its own directory) --
# node_modules is real (npm install), never committed; renders/ and
# snapshots/ are generated output (rendered alpha MOVs, preview PNGs).
graphics/node_modules/
graphics/renders/
graphics/snapshots/
*.mov
